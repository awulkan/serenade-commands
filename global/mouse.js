serenade.global().command(
  "mo",
  async (api, matches) => {
    await api.click("left");
  }
);

serenade.global().command(
  "touch",
  async (api, matches) => {
    await api.click("left");
  }
);
